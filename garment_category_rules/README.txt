使用说明（可编辑）：
1）请先人工核对 all_third_col_categories.txt，把其中的词分拣到：
   - topwear.txt（上装，返回 1）
   - bottoms.txt（下装，返回 2）
   - sets.txt（连衣裙/成套，返回 3）
2）每个 txt 一行一个分类词；如有等价别名可在同一行用英文逗号分隔：例如「T恤,Tee,短袖T」。
3）运行 category_mapper.py 提供的函数或命令行完成分类数值返回。
